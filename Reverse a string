class Solution {
    public void reverseString(char[] s) {
        int f=0, l=s.length-1;
        int n = s.length;
        char temp ;
        while(f<l){
            temp = s[f];
            s[f] = s[l];
            s[l] = temp;
            f++;
            l--;
        }
    }
}
